package org.gatorgradle.internal;

// TODO: add a way to dynamically load code here to download and install a dependency,
//       to allow for arbitrary dependencies
public enum Dependency { GATORGRADER, MDL, HTMLHINT }
